"""
"appending means to add file to the next of the word which can be work as    important is if "a" is used that means we
are writing not read while a+ means we can write and read both
"""

with open("fruit.txt","a") as myfile:
    myfile.write("\nstrawberry")